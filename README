jackmatrix
Matrix layout control for jack audio connections (git version)

Requirements:
- automake
- pkg-config
- gtk+
- jack

To build:
autoreconf --install
./configure
make

To run (once built):
cd src
./jackmatrix

To install:
make install (usually as root)

To uninstall:
make uninstall (as root if you installed as root)
